name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Simulate Build
        run: echo "✅ Build successful (simulation)"

      - name: Simulate Tests
        run: echo "✅ Tests passed (simulation)"

      - name: Simulate Deploy
        run: echo "✅ Deployment successful (simulation)"

